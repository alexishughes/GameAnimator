<UserControl x:Class="KeyframePartialApp.ctrCell"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KeyframePartialApp"
             mc:Ignorable="d" 
>
    <UserControl.Resources>
        <RoutedUICommand x:Key="MakeKeyCell" Text="Make Key Cell" />
    </UserControl.Resources>
    <UserControl.CommandBindings>
        <CommandBinding Command="{StaticResource MakeKeyCell}"  CanExecute="CommandBinding_CanExecute" Executed="MakeKeyCell_Executed"></CommandBinding>
    </UserControl.CommandBindings>

    <Border x:Name="bdrBackground" Width="16" Height="24" BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkDarkBrushKey}}" BorderThickness="1" Background="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" >
        <Border.ContextMenu>
            <ContextMenu>
                <MenuItem Command="{StaticResource MakeKeyCell}"></MenuItem>
            </ContextMenu>
        </Border.ContextMenu>
        <Rectangle Width="12" Height="12" x:Name="rctIcon" />
    </Border>

</UserControl>
